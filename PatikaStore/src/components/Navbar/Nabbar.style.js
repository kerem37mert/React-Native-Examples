import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        padding: 40,
    },
    title: {
        fontWeight: "bold",
        fontSize: 30,
        color: "purple",
        marginBottom: 20
    },
    input: {
        backgroundColor: "#e0e0e0",
        padding: 10,
        borderRadius: 10
    }
});